version: '3'

tasks:
  default:
    cmds:
      - task --list

  status:
    desc: Show application workspace status
    cmds:
      - git status --short

  backend:dev:
    desc: Start the backend in development mode
    dir: services/backend
    cmds:
      - task dev

  backend:test:
    desc: Run backend tests
    dir: services/backend
    cmds:
      - task test

  backend:verify:
    desc: Run the backend verification pipeline
    dir: services/backend
    cmds:
      - task verify

  backend:image:
    desc: Build and push the backend container image
    dir: services/backend
    cmds:
      - task image

  backend:image:build:
    desc: Build the backend container image
    dir: services/backend
    cmds:
      - task image:build

  backend:image:push:
    desc: Push the backend container image
    dir: services/backend
    cmds:
      - task image:push

  backend:image:release:
    desc: Release the backend container image
    dir: services/backend
    cmds:
      - task image:release

  frontend:dev:
    desc: Start the frontend in development mode
    dir: apps/frontend
    cmds:
      - task dev

  frontend:build:
    desc: Build the frontend
    dir: apps/frontend
    cmds:
      - task build

  frontend:test:
    desc: Run frontend tests
    dir: apps/frontend
    cmds:
      - task test

  frontend:check:
    desc: Run frontend type checks
    dir: apps/frontend
    cmds:
      - task check

  frontend:verify:
    desc: Run the frontend verification pipeline
    dir: apps/frontend
    cmds:
      - task verify
